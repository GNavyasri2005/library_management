# PROJECT DEVELOPMENT STEPS
    1. CREATE FOLDER
        ```mkdir library```
    2. CREATE VIRTUAL ENVIRONMENT
    3. ACTIVATE VIRTUAL ENVIRONMENT 
    4. ADD PRE REQUIREMENTS TO requirements.txt
    5. INSTALL PRE REQUIREMENTS
    6. ADD VIRTUAL ENVIRONMENTS NAME TO .gitignorefile
    7. IMPLEMENT LIBRARY SYSTEM
**Step 1:**<br>
        1.create folder command:<br><b>
        ```mkdir library```<br>
**Step 2:**<br>
        2.create virtual environment:<br>
        ```python -m venv library_env```<br>
**Step 3:**<br>
        3.Activate virtual environment<br>
        ```library_env\Scripts\activate```<br>
    - create ```requirements.txt``` file<br>
**Step 4:**<br>
        4.Add pre requirements to requirements.txt<br>
**Step 5:**<br>
        5.Install pre requirements <br>
        ``` pip install -r requirements.txt```<br>
    - create ``` .gitignore``` file<br>
**Step 6:**<br>
        6.Add virtual environment name to .gitignorefile<br>
**Step 7:**<br>
        7.Implement library management system<br>

#intialize repository

git init
git add .
git commit -m "message"
git branch -M main
git remote add origin <repositorylink>.git
git push -u origin main

-Add new changes
git add .
git commit -m "message"
git push -u origin main




